// variavel global
// todo_onibus_ponto			contador pra checar se todos os onibus estao em um ponto (regiao critica - PRECISA DE MUTEX)
// todo_passageiro_ponto		contador pra checar se todos os passageiros estao em um ponto	


/**
* pthread_t passageiros[numero_passageiros]
* pthread_t onibus[numero_oninubs]
* pthread_t ponto[numero_pontos]
*
* struct passageiros[numero_passageiros]
* struct onibus[numero_onibus]
* struct ponto[numero_pontos]
* 
*/

main(void)
{
	cria_pontos();
	cria_passageiros();
	cria_onibus();

	pthread_join(todoas as thread filhas);
	return 0;
}
